//Write a C program to input marks (out of 100) and display them in the given format.
#include<stdio.h>
int main(){
    int marks;
    printf("enter a marks (out of 100)=");
    scanf("%d",&marks);
    printf("Marks=%d",marks);
    return 0;
}